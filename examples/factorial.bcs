extrn func putint(i32): void;

func factorial(n i32): i32 {
    if n <= 1 {
        return 1;
    } else {
        return n * factorial(n - 1);
    }
}


func main(): void {
    let result: i32 = factorial(9);
    putint(result);
}
